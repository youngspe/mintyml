{
  "name": "mintyml",
  "version": "0.1.2",
  "description": "An minimalist alternative syntax to HTML",
  "main": "out-js/index.js",
  "repository": {
    "url": "git+https://github.com/youngspe/mintyml.git"
  },
  "scripts": {
    "build": "wasm-pack build --no-pack -d pkg-web -t bundler && wasm-pack build --no-pack -d pkg-node -t nodejs && tsc",
    "build-web": "wasm-pack build --no-pack -d pkg-web -t bundler && tsc",
    "build-node": "wasm-pack build --no-pack -d pkg-node -t nodejs && tsc"
  },
  "author": "Spencer Young",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
